node_modules

# Environment variables
.env
.env.local
.env.*.local
server/.env

.DS_Store
.vercel
.env*.local

# Scraped data (large, regenerable — run npm run scrape to recreate)
scripts/wine_data.json
